naftiko: 1.0.0-alpha2
info:
  label: LangSmith — tags
  description: 'LangSmith — tags. 5 operations. Lead operation: Get Tags. Self-contained Naftiko capability covering one Langsmith
    business surface.'
  tags:
  - Langsmith
  - tags
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    LANGSMITH_API_KEY: LANGSMITH_API_KEY
capability:
  consumes:
  - type: http
    namespace: langsmith-tags
    baseUri: ''
    description: LangSmith — tags business capability. Self-contained, no shared references.
    resources:
    - name: api-v1-repos-owner-repo-tags
      path: /api/v1/repos/{owner}/{repo}/tags
      operations:
      - name: gettagsapiv1reposownerrepotagsget
        method: GET
        description: Get Tags
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: repo
          in: path
          type: string
          required: true
      - name: createtagapiv1reposownerrepotagspost
        method: POST
        description: Create Tag
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: owner
          in: path
          type: string
          required: true
        - name: repo
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: api-v1-repos-owner-repo-tags-tag_name
      path: /api/v1/repos/{owner}/{repo}/tags/{tag_name}
      operations:
      - name: gettagapiv1reposownerrepotagstagnameget
        method: GET
        description: Get Tag
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: repo
          in: path
          type: string
          required: true
        - name: tag_name
          in: path
          type: string
          required: true
      - name: updatetagapiv1reposownerrepotagstagnamepatch
        method: PATCH
        description: Update Tag
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: owner
          in: path
          type: string
          required: true
        - name: repo
          in: path
          type: string
          required: true
        - name: tag_name
          in: path
          type: string
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletetagapiv1reposownerrepotagstagnamedelete
        method: DELETE
        description: Delete Tag
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: owner
          in: path
          type: string
          required: true
        - name: repo
          in: path
          type: string
          required: true
        - name: tag_name
          in: path
          type: string
          required: true
    authentication:
      type: bearer
      token: '{{env.LANGSMITH_API_KEY}}'
  exposes:
  - type: rest
    namespace: langsmith-tags-rest
    port: 8080
    description: REST adapter for LangSmith — tags. One Spectral-compliant resource per consumed operation, prefixed with
      /v1.
    resources:
    - path: /v1/api/v1/repos/{owner}/{repo}/tags
      name: api-v1-repos-owner-repo-tags
      description: REST surface for api-v1-repos-owner-repo-tags.
      operations:
      - method: GET
        name: gettagsapiv1reposownerrepotagsget
        description: Get Tags
        call: langsmith-tags.gettagsapiv1reposownerrepotagsget
        with:
          repo: rest.repo
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createtagapiv1reposownerrepotagspost
        description: Create Tag
        call: langsmith-tags.createtagapiv1reposownerrepotagspost
        with:
          owner: rest.owner
          repo: rest.repo
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/api/v1/repos/{owner}/{repo}/tags/{tag-name}
      name: api-v1-repos-owner-repo-tags-tag-name
      description: REST surface for api-v1-repos-owner-repo-tags-tag_name.
      operations:
      - method: GET
        name: gettagapiv1reposownerrepotagstagnameget
        description: Get Tag
        call: langsmith-tags.gettagapiv1reposownerrepotagstagnameget
        with:
          repo: rest.repo
          tag_name: rest.tag_name
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: updatetagapiv1reposownerrepotagstagnamepatch
        description: Update Tag
        call: langsmith-tags.updatetagapiv1reposownerrepotagstagnamepatch
        with:
          owner: rest.owner
          repo: rest.repo
          tag_name: rest.tag_name
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletetagapiv1reposownerrepotagstagnamedelete
        description: Delete Tag
        call: langsmith-tags.deletetagapiv1reposownerrepotagstagnamedelete
        with:
          owner: rest.owner
          repo: rest.repo
          tag_name: rest.tag_name
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: langsmith-tags-mcp
    port: 9090
    transport: http
    description: MCP adapter for LangSmith — tags. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: get-tags
      description: Get Tags
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: langsmith-tags.gettagsapiv1reposownerrepotagsget
      with:
        repo: tools.repo
      outputParameters:
      - type: object
        mapping: $.
    - name: create-tag
      description: Create Tag
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: langsmith-tags.createtagapiv1reposownerrepotagspost
      with:
        owner: tools.owner
        repo: tools.repo
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-tag
      description: Get Tag
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: langsmith-tags.gettagapiv1reposownerrepotagstagnameget
      with:
        repo: tools.repo
        tag_name: tools.tag_name
      outputParameters:
      - type: object
        mapping: $.
    - name: update-tag
      description: Update Tag
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: langsmith-tags.updatetagapiv1reposownerrepotagstagnamepatch
      with:
        owner: tools.owner
        repo: tools.repo
        tag_name: tools.tag_name
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-tag
      description: Delete Tag
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: langsmith-tags.deletetagapiv1reposownerrepotagstagnamedelete
      with:
        owner: tools.owner
        repo: tools.repo
        tag_name: tools.tag_name
      outputParameters:
      - type: object
        mapping: $.
