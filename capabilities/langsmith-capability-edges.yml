slug: langsmith
provider: LangSmith
generated_by: planning/capability-mapping/scripts/classify_capabilities.py
model: claude-opus-5
frame:
- Software & Technology
min_confidence: 0.7
capability_model:
  source: https://github.com/vincentmakes/turbo-ea-capabilities
  license: CC-BY-4.0
  attribution: Turbo EA Capabilities by Vincent Verdet — Turbo EA, https://github.com/vincentmakes/turbo-ea-capabilities,
    CC BY 4.0
  notice: NOTICE
edge_count: 14
edges:
- tag: nps
  spec_file: langsmith-nps-api-openapi.yml
  capability_id: BC-430.50
  capability_id_l1: BC-430
  capability_name: Customer Feedback Management
  confidence: 0.85
  evidence: POST /v1/platform/nps/response 'Submit an NPS response'; nps.SubmitResponseRequest
  reason: The single operation captures Net Promoter Score survey responses from users, which is precisely
    voice-of-customer / feedback capture (BC-430.50 names NPS explicitly). Narrow surface but unambiguous.
- tag: access_policies
  spec_file: langsmith-access-policies-api-openapi.yml
  capability_id: BC-620.20
  capability_id_l1: BC-620
  capability_name: Identity & Access Management
  confidence: 0.8
  evidence: POST /v1/platform/orgs/current/access-policies  Create an access policy; Attach access policies
    to a role; schemas authz_internal.Permission, authz_internal.AbacOperator
  reason: Attribute-based access policies, roles and permissions — authorisation administration, squarely
    identity and access management.
- tag: evaluators
  spec_file: langsmith-evaluators-api-openapi.yml
  capability_id: BC-610.60
  capability_id_l1: BC-610
  capability_name: Artificial Intelligence Management
  confidence: 0.8
  evidence: POST /v1/platform/evaluators — Create evaluator; schemas evaluators.LLMEvaluator, evaluators.CodeEvaluator,
    evaluators.EvaluatorRunRule
  reason: Managing LLM and code evaluators that score model outputs is core AI/ML model evaluation and
    quality tooling, i.e. AI model lifecycle / MLOps management.
- tag: tracer-sessions
  spec_file: langsmith-tracer-sessions-api-openapi.yml
  capability_id: BC-4220.20
  capability_id_l1: BC-4220
  capability_name: Observability Management
  confidence: 0.8
  evidence: POST /api/v1/sessions/{session_id}/dashboard  Get Tracing Project Prebuilt Dashboard; read_tracer_sessions_runs_metadata;
    schemas TracerSession, HighlightedRun, RunCluster
  reason: Tracer sessions are tracing projects with dashboards, filter views and run metadata/clustering
    — i.e. management of traces and metrics that make a running application understandable, which is observability
    management.
- tag: aws_marketplace
  spec_file: langsmith-aws-marketplace-api-openapi.yml
  capability_id: BC-4270.70
  capability_id_l1: BC-4270
  capability_name: Software Marketplace Listing Management
  confidence: 0.75
  evidence: POST /aws-marketplace/register — "AWS Marketplace fulfillment URL registration"
  reason: Registration of the AWS Marketplace fulfilment URL is the mechanics of listing/fulfilling the
    SaaS offering via a third-party cloud marketplace. Single thin operation, so not maximal confidence.
- tag: prompt-webhooks
  spec_file: langsmith-prompt-webhooks-api-openapi.yml
  capability_id: BC-4270.80
  capability_id_l1: BC-4270
  capability_name: Webhook & Event Subscription Management
  confidence: 0.75
  evidence: POST /api/v1/prompt-webhooks 'Create Prompt Webhook'; EPromptWebhookTrigger; PromptWebhookPayload;
    POST /api/v1/prompt-webhooks/test 'Test Prompt Webhook'
  reason: Full lifecycle of outbound webhook subscriptions with event triggers, payload schema and delivery
    testing — the candidate list has an explicit capability for webhook and event subscription management,
    so this is a named capability rather than undifferentiated plumbing.
- tag: tenant
  spec_file: langsmith-tenant-api-openapi.yml
  capability_id: BC-4230.10
  capability_id_l1: BC-4230
  capability_name: Tenant Provisioning & Lifecycle
  confidence: 0.72
  evidence: POST /api/v1/tenants create_tenant_api_v1_tenants_post  Create Tenant; schemas TenantCreate,
    TenantForUser
  reason: Operations create and list tenants of the multi-tenant LangSmith service, which is tenant provisioning/lifecycle
    rather than any end-business function. Confidence held below 0.8 because the surface is only two operations
    and 'tenant' here doubles as the vendor's organisation concept.
- tag: agents
  spec_file: langsmith-agents-api-openapi.yml
  capability_id: BC-610.60
  capability_id_l1: BC-610
  capability_name: Artificial Intelligence Management
  confidence: 0.7
  evidence: POST /v1/fleet/agents  Create an agent; schemas agents.AgentConfig, agents.AgentMetadata
  reason: Create/update/list AI agents in the Fleet service with configuration and metadata — lifecycle
    management of AI/LLM agents, i.e. AI/MLOps management.
- tag: datasets
  spec_file: langsmith-datasets-api-openapi.yml
  capability_id: BC-610.60
  capability_id_l1: BC-610
  capability_name: Artificial Intelligence Management
  confidence: 0.7
  evidence: create_dataset_api_v1_datasets_post, upload_csv_dataset, upload_experiment, get_dataset_versions,
    diff_dataset_versions
  reason: Datasets in LangSmith are versioned evaluation datasets for LLM applications — part of the AI/ML
    model evaluation lifecycle (MLOps). Maps to Artificial Intelligence Management; some ambiguity vs
    generic data management.
- tag: experiments
  spec_file: langsmith-experiments-api-openapi.yml
  capability_id: BC-610.60
  capability_id_l1: BC-610
  capability_name: Artificial Intelligence Management
  confidence: 0.7
  evidence: POST /api/v1/runs/experiments/{experiment_id}/evaluate — Evaluate Experiment Adhoc; schema
    EvaluateExperimentRequest
  reason: Triggering evaluation of an LLM experiment run is AI/ML model evaluation within the model lifecycle.
    Single operation, so moderate confidence on the sub-capability; it is not product A/B testing.
- tag: fleet auth
  spec_file: langsmith-fleet-auth-api-openapi.yml
  capability_id: BC-620.20
  capability_id_l1: BC-620
  capability_name: Identity & Access Management
  confidence: 0.7
  evidence: POST /v1/fleet/auth-providers — Register an OAuth provider; POST /v1/fleet/auth-agents/{agent_id}/connections
    — Grant a connection token to an agent; DELETE .../connections/{connection_id} — Revoke an agent's
    connection
  reason: Operations register OAuth providers and grant/revoke connection tokens and authorization sessions
    for agents — identity and access management plumbing. Mapped to IAM, though it is arguably pure authentication
    infrastructure.
- tag: gateway
  spec_file: langsmith-gateway-api-openapi.yml
  capability_id: BC-610.60
  capability_id_l1: BC-610
  capability_name: Artificial Intelligence Management
  confidence: 0.7
  evidence: POST /openai/v1/chat/completions  Proxy OpenAI Chat Completions through the LLM Gateway; POST
    /anthropic/v1/messages  Proxy Anthropic Messages through the LLM Gateway
  reason: An LLM gateway that proxies inference calls to multiple model providers is centralised AI model
    serving/routing, which falls under Artificial Intelligence Management. No schemas provided, so not
    maximal confidence.
- tag: orgs
  spec_file: langsmith-orgs-api-openapi.yml
  capability_id: BC-4230
  capability_id_l1: BC-4230
  capability_name: SaaS Tenant Management
  confidence: 0.7
  evidence: POST /api/v1/orgs 'Create Organization'; GET /api/v1/orgs/current/billing 'Get Organization
    Billing Info'; UpsertTTLSettingsRequest, orgs.WorkspaceMembership
  reason: 'Manages tenant organisations in the multi-tenant SaaS: creation, org info, workspace membership,
    roles, API keys, retention (TTL) settings, plus billing/payment-method attachment. Squarely SaaS Tenant
    Management at L1; the surface spans provisioning, configuration and billing so no single L2 is named.'
- tag: workspaces
  spec_file: langsmith-workspaces-api-openapi.yml
  capability_id: BC-4230.10
  capability_id_l1: BC-4230
  capability_name: Tenant Provisioning & Lifecycle
  confidence: 0.7
  evidence: POST /api/v1/workspaces  Create Workspace; DELETE /api/v1/workspaces/{workspace_id}  Delete
    Workspace; Get Current Workspace Usage Limits Info; schemas TenantStats, MemberIdentity
  reason: Create/patch/delete of workspaces plus workspace stats, limits, secrets and member identities
    is lifecycle management of tenant containers in a multi-tenant SaaS. Breadth beyond provisioning (secrets,
    members, tags) tempers the L2 confidence.
